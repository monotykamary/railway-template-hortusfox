import{readFileSync}from"node:fs";import assert from"node:assert/strict";const a=readFileSync("app/Dockerfile","utf8"),e=readFileSync("app/entrypoint.sh","utf8"),s=readFileSync("app/apache-start.sh","utf8"),m=readFileSync("mariadb/Dockerfile","utf8"),r=readFileSync("README.md","utf8");assert.match(a,/hortusfox-web:v6\.1@sha256:[a-f0-9]{64}/);assert.match(m,/mariadb:11\.8\.9@sha256:[a-f0-9]{64}/);assert.doesNotMatch(`${a}${m}`,/:latest/);for(const p of ['images','attachments','logs','backups','themes','migrations'])assert.match(e,new RegExp(`persist_dir ${p}`));assert.match(e,/hortusfox-upstream-entrypoint/);assert.match(e,/export APP_DEBUG=true/);assert.match(s,/APP_DEBUG=false/);assert.match(s,/a2enmod mpm_prefork/);assert.match(r,/one `\/data` volume/);console.log("static template checks passed");
